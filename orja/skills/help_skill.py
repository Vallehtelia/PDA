from __future__ import annotations


def help_skill(_: str) -> str:
    return (
        "Wake word: 'hey slave'. Available: "
        "time → current time, "
        "help → this message, "
        "timer → set a timer (placeholder)."
    )

